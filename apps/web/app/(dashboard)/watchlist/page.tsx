import { CouncilScan } from "@/components/dashboard/CouncilScan";

export default function WatchlistPage() {
  return (
    <CouncilScan
      title="Watchlist"
      description="View every stock you track, add new tickers, and run local buy / hold / sell analysis across the full list."
    />
  );
}
